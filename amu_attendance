CREATE DATABASE amu_attendance;

USE amu_attendance;

CREATE TABLE student_attendance (
    id INT AUTO_INCREMENT PRIMARY KEY,
    enrollment_number VARCHAR(50) NOT NULL,
    faculty_number VARCHAR(50) NOT NULL,
    cumulative_attendance_percentage DECIMAL(5, 2) NOT NULL
);

-- Insert sample data
INSERT INTO student_attendance (enrollment_number, faculty_number, cumulative_attendance_percentage)
VALUES ('123456', 'FAC001', 85.00);
